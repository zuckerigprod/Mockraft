{
  "name": "mockraft",
  "private": true,
  "version": "1.0.0",
  "description": "AI-Powered API Mocking Tool",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:core": "npm run build -w packages/core",
    "build:cli": "npm run build -w packages/cli",
    "build:web": "npm run build -w packages/web",
    "dev": "npm run dev -w packages/web",
    "server": "npx tsx packages/web/server.ts",
    "start": "npm run build:web && npx tsx packages/web/server.ts",
    "test": "npm run test --workspaces --if-present"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "tsx": "4.21.0"
  }
}
